os: windows
language: shell
jobs:
    - stage: build
      script:
        - git submodule update --init --recursive
        - choco install autohotkey.install --params='/DefaultVer:A32' -y
        - powershell -Command .\\COMPILE.exe
